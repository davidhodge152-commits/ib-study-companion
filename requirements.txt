# Vercel deployment dependencies (trimmed for 250MB serverless limit)
# Full list: requirements-full.txt
flask>=3.0.0
flask-login>=0.6.3
flask-wtf>=1.2.0
# flask-limiter, flask-babel, flask-compress, whitenoise: skipped on Vercel
# (no-op stubs used instead to reduce cold start)
python-dotenv>=1.0.0
tenacity>=8.2.0
# Primary AI provider (google-generativeai excluded — too large with grpcio)
anthropic>=0.39.0
openai>=1.50.0
# Stripe payment processing
stripe>=8.0.0
# OAuth authentication
authlib>=1.3.0
# PostgreSQL support (Supabase)
psycopg2-binary>=2.9.0
