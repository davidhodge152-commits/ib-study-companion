# =============================================================================
# IB Study Companion — Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.

# ---------------------------------------------------------------------------
# Core Flask
# ---------------------------------------------------------------------------

# Flask secret key — REQUIRED in production
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-in-production

# Flask environment: development / production / testing
FLASK_ENV=development

# Database path (defaults to ./ib_study.db)
# DATABASE_URL=./ib_study.db

# ---------------------------------------------------------------------------
# AI Providers
# ---------------------------------------------------------------------------

# Google Gemini API key — primary AI provider
# Get yours at https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_api_key_here

# Anthropic API key (optional — used by some agents)
# ANTHROPIC_API_KEY=

# OpenAI API key (optional — used by some agents)
# OPENAI_API_KEY=

# ---------------------------------------------------------------------------
# Web Push Notifications (VAPID)
# ---------------------------------------------------------------------------
# Generate key pair with: python vapid_keys.py

# VAPID_PRIVATE_KEY=
# VAPID_PUBLIC_KEY=
# VAPID_CLAIMS_EMAIL=mailto:admin@example.com

# ---------------------------------------------------------------------------
# Logging
# ---------------------------------------------------------------------------

# Log format: "text" (human-readable) or "json" (structured, for production)
# LOG_FORMAT=text

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# ---------------------------------------------------------------------------
# Future Integrations (not yet active)
# ---------------------------------------------------------------------------

# Redis URL for caching/sessions
# REDIS_URL=redis://localhost:6379/0

# Sentry DSN for error tracking
# SENTRY_DSN=

# Stripe secret key for payments
# STRIPE_SECRET_KEY=
